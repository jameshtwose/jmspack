[distutils]
  index-servers =
    pypi
    jmspack

[pypi]
  username = __token__
  password = ${{ secrets.PYPI_API_TOKEN }}

[PROJECT_NAME]
  repository = https://upload.pypi.org/legacy/
  username = __token__
  password = ${{ secrets.PYPI_API_TOKEN }}
